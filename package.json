{
  "name": "attackdigital",
  "version": "0.0.1",
  "description": "Attack Digital Website",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "babel-watch": "babel src --watch --out-dir build",
    "babel-build": "babel src --out-dir build",
    "node-watch": "nodemon build/index.js",
    "dev": "concurrently 'npm run babel-watch' 'npm run node-watch'",
    "build": "npm run babel-build",
    "start": "node build/index.js"
  },
  "repository": "https://ryardley@bitbucket.org/ryardley/attackdigital.git",
  "author": "Rudi Yardley <contact@rudiyardley.com> (http://rudiyardley.com)",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^2.2.0",
    "express": "^4.14.0"
  },
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-es2015": "^6.9.0"
  }
}
